!!!
%html
  %head
    %title My Latest Web App
    = stylesheet_link_tag 'web_app_theme'
    = javascript_include_tag :defaults, :cache => true 
    = javascript_include_tag "application", :debug => true
    = csrf_meta_tag
  %body
    #container
      #header
        %h1
          %a{:href => "/"} My Latest Web App
        #user-navigation
          %ul.wat-cf
            %li
              %a{:href => "#"}= t("web-app-theme.profile", :default => "Profile")
            %li
              = link_to t("web-app-theme.Blog-Entry", :default => "Blogs"),blog_entries_path
            %li
              = link_to t("web-app-theme.signout", :default => "Sign out"), destroy_user_session_path, :method => :delete
        #main-navigation
          %ul.wat-cf
      #wrapper.wat-cf
        .flash
          - flash.each do |type, message|
            %div{:class => "message #{type}"}
              %p= message
        #main
          = yield
          #footer
            .block
              %p
                Copyright &copy; #{Time.now.year} My Latest Web App.
        #sidebar
          = yield :sidebar
